# TEST CASES — Calculadora de Nómina

| HU asociada | ID del Caso | Nombre del test | Prioridad | Escenario | Precondiciones | Datos | Pasos de ejecución | Resultado esperado | Estado | Resultado ejecución |
|---|---|---|---|---|---|---|---|---|---|---|
| HU-01 | TC-001 | Registro exitoso con contrato tiempo completo | Crítico | **Dado** que no existe el empleado en el sistema<br>**Cuando** se registra un empleado con sus datos necesarios (nombre, tipo de contrato y salario bruto)<br>**Entonces** el empleado queda guardado en el sistema<br>**Y** queda habilitado para confirmar su nómina | Ambiente de QA disponible y estable<br>No existe previamente un empleado con los mismos datos | nombre = "Juan Fernando Quintero"<br>tipo_contrato = "tiempo_completo"<br>salario_bruto = 3000.00 | 1. `POST /empleados` con los datos de entrada<br>2. Verificar código de respuesta HTTP<br>3. Verificar `id`, `nombre`, `tipo_contrato` y `salario_bruto` en respuesta<br>4. `GET /empleados/{id}` con el ID retornado<br>5. Verificar que los datos guardados coinciden con los enviados | `POST /empleados` retorna HTTP 201<br>Body contiene `id`, `nombre`, `tipo_contrato` y `salario_bruto` correctos<br>`GET /empleados/{id}` retorna HTTP 200 con los mismos datos | Sin ejecutar | — |
| HU-01 | TC-002 | Registro exitoso con contrato medio tiempo | Crítico | **Dado** que no existe el empleado en el sistema<br>**Cuando** se registra un empleado con sus datos necesarios (nombre, tipo de contrato y salario bruto)<br>**Entonces** el empleado queda guardado en el sistema<br>**Y** queda habilitado para confirmar su nómina | Ambiente de QA disponible y estable<br>No existe previamente un empleado con los mismos datos | nombre = "Gonzalo Martinez"<br>tipo_contrato = "medio_tiempo"<br>salario_bruto = 1500.00 | 1. `POST /empleados` con los datos de entrada<br>2. Verificar código de respuesta HTTP<br>3. Verificar `id`, `nombre`, `tipo_contrato` y `salario_bruto` en respuesta<br>4. `GET /empleados/{id}` con el ID retornado<br>5. Verificar que los datos guardados coinciden con los enviados | `POST /empleados` retorna HTTP 201<br>Body contiene `id`, `nombre`, `tipo_contrato = "medio_tiempo"` y `salario_bruto` correctos<br>`GET /empleados/{id}` retorna HTTP 200 con los mismos datos | Sin ejecutar | — |
| HU-01 | TC-003 | Registro exitoso con contrato servicios profesionales | Crítico | **Dado** que no existe el empleado en el sistema<br>**Cuando** se registra un empleado con sus datos necesarios (nombre, tipo de contrato y salario bruto)<br>**Entonces** el empleado queda guardado en el sistema<br>**Y** queda habilitado para confirmar su nómina | Ambiente de QA disponible y estable<br>No existe previamente un empleado con los mismos datos | nombre = "Lucas Pratto"<br>tipo_contrato = "servicios_profesionales"<br>salario_bruto = 5000.00 | 1. `POST /empleados` con los datos de entrada<br>2. Verificar código de respuesta HTTP<br>3. Verificar `id`, `nombre`, `tipo_contrato` y `salario_bruto` en respuesta<br>4. `GET /empleados/{id}` con el ID retornado<br>5. Verificar que los datos guardados coinciden con los enviados | `POST /empleados` retorna HTTP 201<br>Body contiene `id`, `nombre`, `tipo_contrato = "servicios_profesionales"` y `salario_bruto` correctos<br>`GET /empleados/{id}` retorna HTTP 200 con los mismos datos | Sin ejecutar | — |
| HU-01 | TC-004 | Registro fallido por nombre vacío | Alto | **Dado** que no existe el empleado en el sistema<br>**Cuando** se intenta registrar un empleado sin ingresar datos necesarios<br>**Entonces** el sistema no permite guardar el empleado<br>**Y** notifica cuales son los datos necesarios | Ambiente de QA disponible y estable | nombre = ""<br>tipo_contrato = "tiempo_completo"<br>salario_bruto = 2000.00 | 1. `POST /empleados` con `nombre = ""`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados` retorna HTTP 400<br>Mensaje indica que el campo `nombre` es obligatorio | Sin ejecutar | — |
| HU-01 | TC-005 | Registro fallido por tipo de contrato vacío | Alto | **Dado** que no existe el empleado en el sistema<br>**Cuando** se intenta registrar un empleado sin ingresar datos necesarios<br>**Entonces** el sistema no permite guardar el empleado<br>**Y** notifica cuales son los datos necesarios | Ambiente de QA disponible y estable<br>No existe previamente un empleado con los mismos datos | nombre = "Luis Torres"<br>tipo_contrato = ""<br>salario_bruto = 2000.00 | 1. `POST /empleados` con `tipo_contrato = ""`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados` retorna HTTP 400<br>Mensaje indica que el campo `tipo_contrato` es obligatorio | Sin ejecutar | — |
| HU-01 | TC-006 | Registro fallido por salario bruto vacío | Alto | **Dado** que no existe el empleado en el sistema<br>**Cuando** se intenta registrar un empleado sin ingresar datos necesarios<br>**Entonces** el sistema no permite guardar el empleado<br>**Y** notifica cuales son los datos necesarios | Ambiente de QA disponible y estable | nombre = "Franco Armani"<br>tipo_contrato = "medio_tiempo"<br>salario_bruto = (vacío) | 1. `POST /empleados` con `salario_bruto` vacío o ausente<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados` retorna HTTP 400<br>Mensaje indica que el campo `salario_bruto` es obligatorio | Sin ejecutar | — |
| HU-01 | TC-007 | Registro fallido por salario bruto en cero | Alto | **Dado** que el empleado no existe en el sistema<br>**Cuando** se intenta registrar al empleado con un salario bruto negativo o en cero<br>**Entonces** el sistema no permite guardar el empleado<br>**Y** notifica que el salario bruto debe ser positivo | Ambiente de QA disponible y estable | nombre = "Gonzalo Montiel"<br>tipo_contrato = "tiempo_completo"<br>salario_bruto = 0 | 1. `POST /empleados` con `salario_bruto = 0`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados` retorna HTTP 400<br>Mensaje indica que el salario bruto debe ser positivo | Sin ejecutar | — |
| HU-01 | TC-008 | Registro fallido por salario bruto negativo | Alto | **Dado** que el empleado no existe en el sistema<br>**Cuando** se intenta registrar al empleado con un salario bruto negativo o en cero<br>**Entonces** el sistema no permite guardar el empleado<br>**Y** notifica que el salario bruto debe ser positivo | Ambiente de QA disponible y estable | nombre = "Jonatan Maidana"<br>tipo_contrato = "medio_tiempo"<br>salario_bruto = -1500.00 | 1. `POST /empleados` con `salario_bruto = -1500.00`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados` retorna HTTP 400<br>Mensaje indica que el salario bruto debe ser positivo | Sin ejecutar | — |
| HU-01 | TC-009 | Registro fallido por nombre con caracteres especiales | Alto | **Dado** que el empleado no consta en el sistema<br>**Cuando** se intenta registrar al empleado con un nombre que contiene caracteres especiales<br>**Entonces** el sistema no permite guardarlo<br>**Y** notifica que el nombre no debe contener caracteres especiales | Ambiente de QA disponible y estable | nombre = "Matias@#!"<br>tipo_contrato = "tiempo_completo"<br>salario_bruto = 2000.00 | 1. `POST /empleados` con `nombre = "Matias@#!"`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados` retorna HTTP 400<br>Mensaje indica que el nombre no debe contener caracteres especiales | Sin ejecutar | — |
| HU-01 | TC-010 | Registro fallido por nombre con caracteres numéricos | Medio | **Dado** que el empleado no consta en el sistema<br>**Cuando** se intenta registrar al empleado con un nombre que contiene caracteres numéricos<br>**Entonces** el sistema no permite guardarlo<br>**Y** notifica que el nombre no debe contener caracteres especiales | Ambiente de QA disponible y estable | nombre = "1234"<br>tipo_contrato = "servicios_profesionales"<br>salario_bruto = 4000.00 | 1. `POST /empleados` con `nombre = "1234"`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados` retorna HTTP 400<br>Mensaje indica que el nombre no debe contener números | Sin ejecutar | — |
| HU-01 | TC-011 | GET empleado existente retorna HTTP 200 | Crítico | **Dado** que existe un empleado registrado en el sistema<br>**Cuando** se consulta el empleado por su identificador<br>**Entonces** el sistema retorna el detalle del empleado con sus contratos asociados | Existe un empleado previamente registrado con al menos un contrato | id = ID de empleado existente | 1. Registrar un empleado y obtener su `id`<br>2. `GET /empleados/{id}`<br>3. Verificar código de respuesta HTTP<br>4. Verificar que la respuesta incluye `nombre`, `tipo_contrato` y `salario_bruto` | `GET /empleados/{id}` retorna HTTP 200<br>Respuesta incluye `nombre`, `tipo_contrato` y `salario_bruto` | Sin ejecutar | — |
| HU-01 | TC-012 | GET empleado inexistente retorna HTTP 404 | Alto | **Dado** que no existe un empleado con el identificador consultado<br>**Cuando** se consulta el empleado por ese identificador<br>**Entonces** el sistema informa que el empleado no fue encontrado | El ID consultado no existe en base de datos | id = 99999 | 1. `GET /empleados/99999`<br>2. Verificar código de respuesta HTTP | `GET /empleados/99999` retorna HTTP 404 | Sin ejecutar | — |
| HU-01 | TC-013 | Alta de contrato exitosa para empleado existente | Crítico | **Dado** que existe un empleado registrado<br>**Cuando** se da de alta un contrato válido para ese empleado<br>**Entonces** el contrato queda asociado al empleado | Existe un empleado registrado con `id` conocido | id = ID de empleado existente<br>tipo_contrato = "tiempo_completo"<br>fecha_inicio = "2025-01-01" | 1. Registrar un empleado y obtener su `id`<br>2. `POST /empleados/{id}/contratos` con los datos del contrato<br>3. Verificar código de respuesta HTTP<br>4. `GET /empleados/{id}` y verificar que el contrato aparece | `POST /empleados/{id}/contratos` retorna HTTP 201<br>El contrato aparece asociado al empleado en el GET | Sin ejecutar | — |
| HU-01 | TC-014 | Alta de contrato fallida por tipo de contrato vacío | Alto | **Dado** que existe un empleado registrado<br>**Cuando** se intenta dar de alta un contrato sin ingresar el tipo de contrato<br>**Entonces** el sistema no permite crear el contrato<br>**Y** notifica que el tipo de contrato es obligatorio | Existe un empleado registrado con `id` conocido | id = ID de empleado existente<br>tipo_contrato = "" | 1. `POST /empleados/{id}/contratos` con `tipo_contrato = ""`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados/{id}/contratos` retorna HTTP 400<br>Mensaje indica que el campo `tipo_contrato` es obligatorio<br>El contrato no es creado | Sin ejecutar | — |
| HU-01 | TC-015 | Alta de contrato fallida si el empleado no existe | Alto | **Dado** que no existe el empleado en el sistema<br>**Cuando** se intenta dar de alta un contrato para ese empleado<br>**Entonces** el sistema informa que el empleado no fue encontrado<br>**Y** no se crea ningún contrato | El ID de empleado utilizado no existe en base de datos | id = 99999<br>tipo_contrato = "tiempo_completo" | 1. `POST /empleados/99999/contratos` con datos válidos de contrato<br>2. Verificar código de respuesta HTTP | `POST /empleados/99999/contratos` retorna HTTP 404<br>Ningún contrato es creado | Sin ejecutar | — |
| HU-03 | TC-016 | Cálculo correcto para contrato tiempo completo | Crítico | **Dado** que existe un empleado registrado con un tipo de contrato de tiempo completo y un salario bruto definido<br>**Cuando** se procesa la nómina del empleado<br>**Entonces** el sistema calcula el salario neto aplicando 9.45% de deducción<br>**Y** 8.33% de bonificación sobre el salario bruto | Existe un empleado registrado con `tipo_contrato = "tiempo_completo"` y `salario_bruto = 3000.00` | id = ID de empleado existente<br>tipo_contrato = "tiempo_completo"<br>salario_bruto = 3000.00<br>---<br>deduccion = 3000.00 × 9.45% = 283.50<br>bonificacion = 3000.00 × 8.33% = 249.90<br>salario_neto = 3000.00 − 283.50 + 249.90 = 2966.40 | 1. Registrar empleado con los datos indicados<br>2. `POST /empleados/{id}/calcularSalario`<br>3. Verificar código de respuesta HTTP<br>4. Verificar valores de deducción, bonificación y salario neto en la respuesta | `POST /empleados/{id}/calcularSalario` retorna HTTP 200<br>`deduccion = 283.50`<br>`bonificacion = 249.90`<br>`salario_neto = 2966.40` | Sin ejecutar | — |
| HU-03 | TC-017 | Cálculo correcto para contrato medio tiempo | Crítico | **Dado** que existe un empleado registrado con un tipo de contrato de medio tiempo y un salario bruto definido<br>**Cuando** se procesa la nómina del empleado<br>**Entonces** el sistema calcula el salario neto aplicando 9.45% de deducción<br>**Y** 8.33% de bonificación sobre el salario bruto | Existe un empleado registrado con `tipo_contrato = "medio_tiempo"` y `salario_bruto = 1500.00` | id = ID de empleado existente<br>tipo_contrato = "medio_tiempo"<br>salario_bruto = 1500.00<br>---<br>deduccion = 1500.00 × 9.45% = 141.75<br>bonificacion = 1500.00 × 8.33% = 124.95<br>salario_neto = 1500.00 − 141.75 + 124.95 = 1483.20 | 1. Registrar empleado con los datos indicados<br>2. `POST /empleados/{id}/calcularSalario`<br>3. Verificar código de respuesta HTTP<br>4. Verificar valores de deducción, bonificación y salario neto en la respuesta | `POST /empleados/{id}/calcularSalario` retorna HTTP 200<br>`deduccion = 141.75`<br>`bonificacion = 124.95`<br>`salario_neto = 1483.20` | Sin ejecutar | — |
| HU-03 | TC-018 | Cálculo correcto para contrato servicios profesionales | Crítico | **Dado** que existe un empleado registrado con contrato de servicios profesionales y un salario bruto definido<br>**Cuando** se procesa la nómina del empleado<br>**Entonces** el sistema calcula el salario neto aplicando 8.00% de deducción<br>**Y** 0% de bonificación sobre el salario bruto | Existe un empleado registrado con `tipo_contrato = "servicios_profesionales"` y `salario_bruto = 5000.00` | id = ID de empleado existente<br>tipo_contrato = "servicios_profesionales"<br>salario_bruto = 5000.00<br>---<br>deduccion = 5000.00 × 8.00% = 400.00<br>bonificacion = 5000.00 × 0% = 0.00<br>salario_neto = 5000.00 − 400.00 + 0.00 = 4600.00 | 1. Registrar empleado con los datos indicados<br>2. `POST /empleados/{id}/calcularSalario`<br>3. Verificar código de respuesta HTTP<br>4. Verificar que `bonificacion = 0.00` y salario neto es correcto | `POST /empleados/{id}/calcularSalario` retorna HTTP 200<br>`deduccion = 400.00`<br>`bonificacion = 0.00`<br>`salario_neto = 4600.00` | Sin ejecutar | — |
| HU-03 | TC-019 | Formato de 2 decimales en tiempo completo | Alto | **Dado** que existe un empleado registrado con un tipo de contrato y salario bruto definidos<br>**Cuando** se procesa la nómina<br>**Entonces** todos los valores numéricos del resultado están redondeados a 2 decimales | Existe un empleado registrado con `salario_bruto` que genera decimales en el cálculo | tipo_contrato = "tiempo_completo"<br>salario_bruto = 1000.00<br>---<br>deduccion = 94.50<br>bonificacion = 83.30<br>salario_neto = 988.80 | 1. `POST /empleados/{id}/calcularSalario`<br>2. Verificar que todos los campos numéricos en la respuesta tienen exactamente 2 decimales | Todos los valores numéricos retornados tienen exactamente 2 decimales | Sin ejecutar | — |
| HU-03 | TC-020 | Redondeo a 2 decimales en servicios profesionales | Alto | **Dado** que existe un empleado registrado con un tipo de contrato y salario bruto definidos<br>**Cuando** se procesa la nómina<br>**Entonces** todos los valores numéricos del resultado están redondeados a 2 decimales | Existe un empleado registrado con `salario_bruto = 333.33` | tipo_contrato = "servicios_profesionales"<br>salario_bruto = 333.33<br>---<br>deduccion = 26.67<br>bonificacion = 0.00<br>salario_neto = 306.66 | 1. `POST /empleados/{id}/calcularSalario`<br>2. Verificar que todos los campos numéricos tienen exactamente 2 decimales | Todos los valores retornados con exactamente 2 decimales | Sin ejecutar | — |
| HU-03 | TC-021 | Cálculo bloqueado si el empleado no existe | Alto | **Dado** que no existe ningún empleado registrado en el sistema<br>**Cuando** el administrador intenta procesar una nómina<br>**Entonces** el sistema no permite realizar el cálculo<br>**Y** informa que debe registrar un empleado antes de continuar | El ID de empleado utilizado no existe en base de datos | id = 99999 | 1. `POST /empleados/99999/calcularSalario`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados/99999/calcularSalario` retorna HTTP 404<br>Mensaje indica que el empleado no existe | Sin ejecutar | — |
| HU-03 | TC-022 | Alta de salario exitosa | Crítico | **Dado** que existe un empleado registrado<br>**Cuando** se da de alta un salario con un valor positivo válido y fecha de vigencia<br>**Entonces** el salario queda asociado al empleado | Existe un empleado registrado con `id` conocido | id = ID de empleado existente<br>salario_bruto = 4000.00<br>fecha_vigencia = "2025-01-01" | 1. `POST /empleados/{id}/salarios` con los datos indicados<br>2. Verificar código de respuesta HTTP<br>3. `GET /empleados/{id}` y verificar que el salario aparece asociado con `salario_bruto = 4000.00` y `fecha_vigencia = "2025-01-01"` | `POST /empleados/{id}/salarios` retorna HTTP 201<br>`GET /empleados/{id}` retorna el salario recién creado con `salario_bruto = 4000.00` y `fecha_vigencia = "2025-01-01"` | Sin ejecutar | — |
| HU-03 | TC-023 | Alta de salario fallida por valor negativo | Alto | **Dado** que existe un empleado registrado<br>**Cuando** se intenta dar de alta un salario con un valor negativo<br>**Entonces** el sistema no permite crear el salario | Existe un empleado registrado con `id` conocido | id = ID de empleado existente<br>salario_bruto = -100.00 | 1. `POST /empleados/{id}/salarios` con `salario_bruto = -100.00`<br>2. Verificar código de respuesta HTTP | `POST /empleados/{id}/salarios` retorna HTTP 400<br>El salario no es creado | Sin ejecutar | — |
| HU-03 | TC-024 | Alta de salario fallida por valor en cero | Alto | **Dado** que existe un empleado registrado<br>**Cuando** se intenta dar de alta un salario con valor en cero<br>**Entonces** el sistema no permite crear el salario | Existe un empleado registrado con `id` conocido | id = ID de empleado existente<br>salario_bruto = 0 | 1. `POST /empleados/{id}/salarios` con `salario_bruto = 0`<br>2. Verificar código de respuesta HTTP<br>3. Verificar el mensaje de error retornado | `POST /empleados/{id}/salarios` retorna HTTP 400<br>Mensaje indica que el salario bruto debe ser positivo<br>El salario no es creado | Sin ejecutar | — |
| HU-03 | TC-025 | Resultado de nómina persistido correctamente | Crítico | **Dado** que existe un empleado con tipo de contrato y salario bruto definidos<br>**Cuando** se procesa la nómina del empleado<br>**Entonces** el resultado queda persistido en nóminas con todos los campos correctos | Existe un empleado registrado con salario y tipo de contrato definidos | tipo_contrato = "tiempo_completo"<br>salario_bruto = 3000.00<br>---<br>salario_bruto esperado = 3000.00<br>deducciones esperadas = 283.50<br>bonificacion esperada = 249.90<br>salario_neto esperado = 2966.40 | 1. `POST /empleados/{id}/calcularSalario`<br>2. Obtener el `idNomina` retornado<br>3. `GET /empleados/{id}/nominas/{idNomina}` y verificar los campos persistidos | `salario_bruto = 3000.00`<br>`deducciones = 283.50`<br>`bonificacion = 249.90`<br>`salario_neto = 2966.40`<br>`fecha_generacion` y `periodo` presentes con valores válidos | Sin ejecutar | — |
| HU-03 | TC-026 | Fecha de vigencia y periodo se guardan correctamente | Medio | **Dado** que existe un empleado con salario registrado con fecha de vigencia definida<br>**Cuando** se procesa la nómina<br>**Entonces** el campo fecha_vigencia y el campo periodo quedan guardados en la nómina | Existe un empleado con salario registrado con `fecha_vigencia` definida | salario_bruto = 2000.00<br>fecha_vigencia = "2025-01-01"<br>periodo = "2025-01" | 1. Registrar salario con `fecha_vigencia = "2025-01-01"`<br>2. `POST /empleados/{id}/calcularSalario`<br>3. Verificar que en la nómina persistida `fecha_vigencia` y `periodo` tienen los valores correctos | La nómina persistida contiene `fecha_vigencia = "2025-01-01"` y `periodo = "2025-01"` | Sin ejecutar | — |
| HU-03 | TC-027 | Redondeo a 2 decimales en medio tiempo | Alto | **Dado** que existe un empleado registrado con un tipo de contrato y salario bruto definidos<br>**Cuando** se procesa la nómina<br>**Entonces** todos los valores numéricos del resultado están redondeados a 2 decimales | Existe un empleado registrado con `salario_bruto` que genera decimales en el cálculo | tipo_contrato = "medio_tiempo"<br>salario_bruto = 777.77<br>---<br>deduccion = 777.77 × 9.45% = 73.50<br>bonificacion = 777.77 × 8.33% = 64.79<br>salario_neto = 777.77 − 73.50 + 64.79 = 769.06 | 1. `POST /empleados/{id}/calcularSalario`<br>2. Verificar que todos los campos numéricos en la respuesta tienen exactamente 2 decimales | Todos los valores numéricos retornados tienen exactamente 2 decimales | Sin ejecutar | — |
| HU-03 | TC-028 `[Performance]` | Tiempo de respuesta del cálculo de nómina ≤ 3 segundos | Alto | **Dado** que existe un empleado registrado con tipo de contrato y salario bruto definidos<br>**Cuando** se solicita el cálculo de la nómina<br>**Entonces** el sistema responde en 3 segundos o menos | Existe un empleado registrado con salario y tipo de contrato definidos<br>Ambiente de QA disponible y estable | id = ID de empleado existente<br>Umbral = 3000ms | 1. `POST /empleados/{id}/calcularSalario`<br>2. Medir el tiempo de respuesta desde el envío de la solicitud hasta recibir la respuesta completa<br>3. Comparar el tiempo medido contra el umbral de 3 segundos | El tiempo de respuesta es ≤ 3000ms<br>La respuesta retorna HTTP 200 dentro del umbral definido en el PRD | Sin ejecutar | — |
| HU-04 | TC-029 | Resumen muestra todos los campos del cálculo | Crítico | **Dado** que la nómina del empleado fue calculada<br>**Cuando** el administrador accede al resumen del cálculo<br>**Entonces** el sistema debe mostrar el nombre, tipo de contrato, salario bruto, deducciones, bonificación y salario neto del empleado | Existe un empleado con nómina ya calculada | id = ID de empleado existente<br>idNomina = ID de nómina calculada | 1. Registrar empleado, calcular su nómina y obtener el `idNomina`<br>2. `GET /empleados/{id}/nominas/{idNomina}`<br>3. Verificar código de respuesta HTTP<br>4. Verificar que la respuesta contiene `nombre`, `tipo_contrato`, `salario_bruto`, `deducciones`, `bonificacion` y `salario_neto` | `GET /empleados/{id}/nominas/{idNomina}` retorna HTTP 200<br>La respuesta incluye los 6 campos requeridos con los valores correctos | Sin ejecutar | — |
| HU-04 | TC-030 | Datos del resumen coinciden con datos persistidos | Crítico | **Dado** que la nómina del empleado fue calculada<br>**Cuando** el administrador accede al resumen del cálculo<br>**Entonces** los valores mostrados en el resumen son idénticos a los persistidos en la tabla nóminas | Existe un empleado con nómina calculada y persistida | id = ID de empleado existente<br>idNomina = ID de nómina calculada | 1. Calcular nómina y registrar los valores retornados por `POST /empleados/{id}/calcularSalario`<br>2. `GET /empleados/{id}/nominas/{idNomina}`<br>3. Comparar cada campo del resumen con los valores calculados previamente | `salario_bruto`, `deducciones`, `bonificacion` y `salario_neto` del resumen son idénticos a los del cálculo<br>No hay diferencias entre lo calculado y lo mostrado | Sin ejecutar | — |
| HU-04 | TC-031 `[E2E - UI]` | Confirmación del resumen habilita descarga de PDF | Crítico | **Dado** que la nómina del empleado fue calculada<br>**Y** el administrador está revisando el resumen en la interfaz<br>**Cuando** el administrador confirma que los datos son correctos<br>**Entonces** el sistema debe habilitar la descarga del PDF | Existe un empleado con nómina calculada<br>El administrador ha accedido a la vista de resumen en la interfaz | id = ID de empleado existente<br>idNomina = ID de nómina calculada | 1. Acceder al resumen de la nómina en la interfaz<br>2. Verificar que el botón de descarga de PDF está **deshabilitado**<br>3. Hacer clic en el botón "Confirmar datos"<br>4. Verificar que el botón de descarga de PDF queda **habilitado** | Antes de confirmar: botón de descarga de PDF deshabilitado o no visible<br>Después de confirmar: botón de descarga de PDF habilitado<br>Estado de la nómina cambia a "confirmada" | Sin ejecutar | — |
| HU-04 | TC-032 | Bloqueo de generación de PDF sin confirmar resumen | Crítico | **Dado** que la nómina del empleado fue calculada<br>**Y** el administrador no confirma los datos<br>**Cuando** el administrador intenta generar el PDF<br>**Entonces** el sistema debe informar que debe confirmar los datos antes de seguir | Existe un empleado con nómina calculada pero resumen NO confirmado | id = ID de empleado existente<br>idNomina = ID de nómina calculada sin confirmar | 1. Calcular la nómina sin confirmar el resumen<br>2. `POST /empleados/{id}/nominas/{idNomina}/generar-pdf`<br>3. Verificar código de respuesta HTTP<br>4. Verificar el mensaje de error retornado | HTTP 400<br>Mensaje indica que debe confirmar los datos antes de generar el PDF | Sin ejecutar | — |
| HU-04 | TC-033 | GET nómina existente retorna desglose completo | Alto | **Dado** que existe una nómina calculada para el empleado<br>**Cuando** se consulta la nómina por su identificador<br>**Entonces** el sistema retorna el desglose completo con todos los campos requeridos | Existe un empleado con nómina calculada | id = ID de empleado existente<br>idNomina = ID de nómina existente | 1. `GET /empleados/{id}/nominas/{idNomina}`<br>2. Verificar código de respuesta HTTP<br>3. Verificar que la respuesta contiene el desglose completo | HTTP 200<br>Desglose incluye `salario_bruto`, `deducciones`, `bonificacion` y `salario_neto` | Sin ejecutar | — |
| HU-04 | TC-034 | GET nómina inexistente retorna HTTP 404 | Alto | **Dado** que no existe una nómina con el identificador consultado<br>**Cuando** se consulta esa nómina<br>**Entonces** el sistema informa que la nómina no fue encontrada | El ID de nómina utilizado no existe en base de datos | id = ID de empleado existente<br>idNomina = 99999 | 1. `GET /empleados/{id}/nominas/99999`<br>2. Verificar código de respuesta HTTP | HTTP 404 | Sin ejecutar | — |
| HU-02 | TC-035 | Corrección exitosa del nombre del empleado | Crítico | **Dado** que el empleado está registrado con los datos necesarios<br>**Y** la nómina no fue confirmada<br>**Cuando** se cambia el nombre del empleado por uno válido<br>**Entonces** los datos del empleado quedan actualizados | Existe un empleado registrado con `id` conocido<br>La nómina del empleado está en estado pendiente (sin calcular) | id = ID de empleado existente<br>nombre_nuevo = "Gonzalo Fernando Martinez" | 1. Registrar un empleado y obtener su `id`<br>2. `PUT /empleados/{id}` con el nuevo nombre<br>3. Verificar código de respuesta HTTP<br>4. `GET /empleados/{id}` y verificar que el nombre fue actualizado | `PUT /empleados/{id}` retorna HTTP 200<br>`GET /empleados/{id}` retorna el nombre actualizado correctamente | Sin ejecutar | — |
| HU-02 | TC-036 | Corrección exitosa del salario bruto | Crítico | **Dado** que el empleado está registrado<br>**Y** la nómina del empleado no fue confirmada<br>**Cuando** se actualiza el salario bruto con un valor positivo válido<br>**Entonces** los datos quedan actualizados con el nuevo valor | Existe un empleado registrado con `id` conocido<br>La nómina del empleado está en estado pendiente (sin calcular) | id = ID de empleado existente<br>salario_bruto_nuevo = 3500.00 | 1. Registrar un empleado con `salario_bruto = 3000.00`<br>2. `PUT /empleados/{id}` con `salario_bruto = 3500.00`<br>3. Verificar código de respuesta HTTP<br>4. `GET /empleados/{id}` y verificar que el salario fue actualizado | `PUT /empleados/{id}` retorna HTTP 200<br>`GET /empleados/{id}` retorna `salario_bruto = 3500.00` | Sin ejecutar | — |
| HU-02 | TC-037 | Corrección fallida por nombre con caracteres especiales | Alto | **Dado** que el empleado está registrado<br>**Y** la nómina del empleado no fue confirmada<br>**Cuando** se intenta cambiar el nombre por uno que contiene caracteres especiales<br>**Entonces** los datos del empleado no quedan actualizados<br>**Y** el sistema informa que el nombre no puede tener caracteres especiales | Existe un empleado registrado con `id` conocido<br>La nómina del empleado no ha sido calculada | id = ID de empleado existente<br>nombre_nuevo = "Carlos@#2025" | 1. `PUT /empleados/{id}` con `nombre = "Carlos@#2025"`<br>2. Verificar código de respuesta HTTP<br>3. Verificar mensaje de error retornado<br>4. `GET /empleados/{id}` y verificar que el nombre original no cambió | `PUT /empleados/{id}` retorna HTTP 400<br>Mensaje indica que el nombre no puede contener caracteres especiales<br>El nombre del empleado permanece sin cambios | Sin ejecutar | — |
| HU-02 | TC-038 | Corrección fallida por salario bruto negativo | Alto | **Dado** que existe un empleado registrado con sus datos<br>**Y** la nómina del empleado no fue confirmada<br>**Cuando** se intenta actualizar el salario bruto con un valor negativo<br>**Entonces** los datos del empleado permanecen sin cambios<br>**Y** el sistema informa que el salario bruto tiene que ser positivo | Existe un empleado registrado con `id` conocido<br>La nómina del empleado no ha sido calculada | id = ID de empleado existente<br>salario_bruto_nuevo = -500.00 | 1. `PUT /empleados/{id}` con `salario_bruto = -500.00`<br>2. Verificar código de respuesta HTTP<br>3. Verificar mensaje de error retornado<br>4. `GET /empleados/{id}` y verificar que el salario original no cambió | `PUT /empleados/{id}` retorna HTTP 400<br>Mensaje indica que el salario bruto debe ser positivo<br>El salario del empleado permanece sin cambios | Sin ejecutar | — |
| HU-02 | TC-039 | Bloqueo de corrección por nómina ya calculada | Crítico | **Dado** que el empleado está registrado<br>**Y** la nómina del empleado fue calculada<br>**Cuando** el administrador intenta modificar datos del empleado<br>**Entonces** el sistema no lo permite<br>**Y** informa que los datos no pueden modificarse porque la nómina ya fue calculada | Existe un empleado registrado<br>La nómina del empleado ya fue calculada y está en estado "calculada" | id = ID de empleado existente<br>nombre_nuevo = "Carlos Sanchez" | 1. Registrar empleado, calcular su nómina<br>2. `PUT /empleados/{id}` con nuevos datos válidos<br>3. Verificar código de respuesta HTTP<br>4. Verificar mensaje de error retornado | `PUT /empleados/{id}` retorna HTTP 403<br>Mensaje indica que los datos no pueden modificarse porque la nómina ya fue calculada | Sin ejecutar | — |
| HU-02 | TC-040 `[Exploratorio]` | Race condition — nómina calculada durante sesión de edición | Medio | **Dado** que el administrador tiene abierto el formulario de edición del empleado<br>**Y** en ese momento la nómina pasa al estado "calculada"<br>**Cuando** el administrador intenta guardar los cambios<br>**Entonces** se documenta el comportamiento observado del sistema | Existe un empleado registrado<br>El formulario de edición está abierto en la interfaz<br>La nómina pasa a estado "calculada" mientras el formulario está activo | Escenario de concurrencia: edición activa + cálculo concurrente | 1. Abrir el formulario de edición del empleado en la interfaz<br>2. Antes de guardar, disparar `POST /empleados/{id}/calcularSalario` desde otra sesión<br>3. Intentar guardar los cambios en el formulario original<br>4. Registrar el código HTTP y el mensaje retornado | Documentar el comportamiento observado: si retorna HTTP 403, HTTP 409 u otro código; si el mensaje es claro para el usuario; si los datos quedan en estado consistente | Sin ejecutar | — |
| HU-02 | TC-041 | Corrección exitosa de contrato | Alto | **Dado** que el empleado está registrado con un contrato asociado<br>**Y** la nómina no fue confirmada<br>**Cuando** se edita el contrato con datos válidos<br>**Entonces** el contrato queda actualizado | Existe un empleado registrado con un contrato asociado (`idContrato` conocido)<br>La nómina del empleado no ha sido calculada | id = ID de empleado existente<br>idContrato = ID de contrato existente<br>tipo_contrato_nuevo = "medio_tiempo" | 1. Registrar un empleado con contrato y obtener `idContrato`<br>2. `PUT /empleados/{id}/contratos/{idContrato}` con el nuevo tipo de contrato<br>3. Verificar código de respuesta HTTP<br>4. `GET /empleados/{id}` y verificar que el contrato fue actualizado | `PUT /empleados/{id}/contratos/{idContrato}` retorna HTTP 200<br>El contrato aparece actualizado en el GET | Sin ejecutar | — |
| HU-02 | TC-042 | Corrección fallida de contrato por tipo vacío | Alto | **Dado** que el empleado está registrado con un contrato asociado<br>**Y** la nómina no fue confirmada<br>**Cuando** se intenta editar el contrato con `tipo_contrato` vacío<br>**Entonces** el sistema no permite la actualización | Existe un empleado registrado con un contrato asociado (`idContrato` conocido)<br>La nómina del empleado no ha sido calculada | id = ID de empleado existente<br>idContrato = ID de contrato existente<br>tipo_contrato = "" | 1. `PUT /empleados/{id}/contratos/{idContrato}` con `tipo_contrato = ""`<br>2. Verificar código de respuesta HTTP | `PUT /empleados/{id}/contratos/{idContrato}` retorna HTTP 400<br>El contrato no es actualizado | Sin ejecutar | — |
| HU-02 | TC-043 | Corrección exitosa de salario | Alto | **Dado** que el empleado está registrado con un salario asociado<br>**Y** la nómina no fue confirmada<br>**Cuando** se edita el salario con un valor positivo válido<br>**Entonces** el salario queda actualizado | Existe un empleado registrado con un salario asociado (`idSalario` conocido)<br>La nómina del empleado no ha sido calculada | id = ID de empleado existente<br>idSalario = ID de salario existente<br>salario_bruto_nuevo = 4500.00 | 1. Registrar un empleado con salario y obtener `idSalario`<br>2. `PUT /empleados/{id}/salarios/{idSalario}` con `salario_bruto = 4500.00`<br>3. Verificar código de respuesta HTTP<br>4. `GET /empleados/{id}` y verificar que el salario fue actualizado a `salario_bruto = 4500.00` | `PUT /empleados/{id}/salarios/{idSalario}` retorna HTTP 200<br>`GET /empleados/{id}` retorna el salario con `salario_bruto = 4500.00` | Sin ejecutar | — |
| HU-02 | TC-044 | Corrección fallida de salario por valor en cero | Alto | **Dado** que el empleado está registrado con un salario asociado<br>**Y** la nómina no fue confirmada<br>**Cuando** se intenta editar el salario con valor en cero<br>**Entonces** el sistema no permite la actualización | Existe un empleado registrado con un salario asociado (`idSalario` conocido)<br>La nómina del empleado no ha sido calculada | id = ID de empleado existente<br>idSalario = ID de salario existente<br>salario_bruto = 0 | 1. `PUT /empleados/{id}/salarios/{idSalario}` con `salario_bruto = 0`<br>2. Verificar código de respuesta HTTP | `PUT /empleados/{id}/salarios/{idSalario}` retorna HTTP 400<br>El salario no es actualizado | Sin ejecutar | — |
| HU-02 | TC-045 `[E2E - UI]` | Mensaje de confirmación tras edición exitosa | Medio | **Dado** que el empleado está registrado<br>**Y** la nómina no fue confirmada<br>**Cuando** el administrador guarda una edición válida en la interfaz<br>**Entonces** el sistema muestra un mensaje de confirmación de cambios | Existe un empleado registrado<br>El administrador tiene el formulario de edición abierto en la interfaz | nombre_nuevo = "Pedro Gomez" | 1. Acceder al formulario de edición del empleado en la interfaz<br>2. Modificar el nombre con un valor válido<br>3. Guardar los cambios<br>4. Verificar que el sistema muestra mensaje de confirmación | El sistema muestra el mensaje de confirmación de cambios exitosos tras guardar | Sin ejecutar | — |
| HU-05 | TC-046 | Generación exitosa del PDF con campos correctos | Crítico | **Dado** que la nómina del empleado fue calculada<br>**Y** el administrador confirmó los datos del resumen<br>**Cuando** el administrador genera el PDF<br>**Entonces** el sistema genera un PDF descargable con los campos correctos | Existe un empleado con nómina calculada y resumen confirmado<br>(`idNomina` conocido) | id = ID de empleado existente<br>idNomina = ID de nómina confirmada<br>Campos esperados en PDF: nombre, tipo_contrato, salario_bruto, deduccion, bonificacion, salario_neto | 1. Registrar empleado, calcular nómina y confirmar resumen<br>2. `POST /empleados/{id}/nominas/{idNomina}/generar-pdf`<br>3. Verificar código de respuesta HTTP<br>4. Verificar que el header `Content-Type` es `application/pdf`<br>5. Verificar que el cuerpo de la respuesta contiene los 6 campos requeridos | `POST /empleados/{id}/nominas/{idNomina}/generar-pdf` retorna HTTP 200<br>Header `Content-Type: application/pdf` presente<br>El PDF contiene: nombre, tipo_contrato, salario_bruto, deduccion, bonificacion y salario_neto con los valores correctos | Sin ejecutar | — |
| HU-05 | TC-047 | Bloqueo de generación de PDF sin confirmar resumen | Crítico | **Dado** que la nómina del empleado fue calculada<br>**Y** el administrador NO confirmó los datos del resumen<br>**Cuando** el administrador intenta generar el PDF<br>**Entonces** el sistema bloquea la operación e informa que debe confirmar primero | Existe un empleado con nómina calculada<br>El resumen NO ha sido confirmado | id = ID de empleado existente<br>idNomina = ID de nómina calculada sin confirmar | 1. Calcular la nómina sin confirmar el resumen<br>2. `POST /empleados/{id}/nominas/{idNomina}/generar-pdf`<br>3. Verificar código de respuesta HTTP<br>4. Verificar mensaje de error retornado | `POST /empleados/{id}/nominas/{idNomina}/generar-pdf` retorna HTTP 400 o HTTP 403<br>Mensaje indica que debe confirmar los datos del resumen antes de generar el PDF | Sin ejecutar | — |
| HU-05 | TC-048 `[E2E - UI]` | Previsualización del PDF muestra todos los campos correctos | Alto | **Dado** que la nómina fue calculada y el resumen fue confirmado<br>**Cuando** el administrador accede a la previsualización del PDF en la interfaz<br>**Entonces** la previsualización muestra todos los campos correctos antes de descargar | Existe un empleado con nómina calculada y resumen confirmado<br>El administrador accede a la vista de previsualización | id = ID de empleado existente<br>idNomina = ID de nómina confirmada | 1. Navegar a la vista de previsualización del PDF<br>2. Verificar que la previsualización muestra: nombre, tipo_contrato, salario_bruto, deduccion, bonificacion y salario_neto<br>3. Verificar que los valores coinciden con los del resumen confirmado | La previsualización muestra los 6 campos correctos con los mismos valores que el resumen confirmado | Sin ejecutar | — |
| HU-05 | TC-049 `[E2E - UI]` | Notificación de descarga exitosa | Alto | **Dado** que la nómina fue calculada y el resumen fue confirmado<br>**Cuando** el administrador descarga el PDF desde la interfaz<br>**Entonces** el sistema muestra una notificación de descarga exitosa | Existe un empleado con nómina calculada y resumen confirmado<br>El administrador está en la vista con el botón de descarga habilitado | id = ID de empleado existente<br>idNomina = ID de nómina confirmada | 1. Hacer clic en el botón de descarga del PDF en la interfaz<br>2. Esperar a que la descarga complete<br>3. Verificar que el sistema muestra la notificación de descarga exitosa | El sistema muestra la notificación de descarga exitosa<br>El archivo PDF queda disponible en el dispositivo del usuario | Sin ejecutar | — |
| HU-05 | TC-050 `[Performance]` | Tiempo de generación del PDF ≤ 3 segundos | Alto | **Dado** que la nómina fue calculada y el resumen fue confirmado<br>**Cuando** se solicita la generación del PDF<br>**Entonces** el sistema responde en 3 segundos o menos | Existe un empleado con nómina calculada y resumen confirmado | id = ID de empleado existente<br>idNomina = ID de nómina confirmada<br>Umbral = 3000ms | 1. `POST /empleados/{id}/nominas/{idNomina}/generar-pdf`<br>2. Medir el tiempo de respuesta desde el envío de la solicitud hasta recibir la respuesta completa<br>3. Comparar el tiempo medido contra el umbral de 3 segundos | El tiempo de respuesta es ≤ 3000ms<br>La respuesta retorna HTTP 200 dentro del umbral definido en el PRD | Sin ejecutar | — |
| HU-05 | TC-051 `[Exploratorio]` | Consistencia visual del PDF en distintos navegadores | Bajo | **Dado** que la nómina fue calculada y el resumen fue confirmado<br>**Cuando** se genera y visualiza el PDF en distintos navegadores<br>**Entonces** se documenta si el PDF se ve igual en todos los navegadores evaluados | Existe un empleado con nómina calculada y resumen confirmado<br>Navegadores disponibles: Chrome, Firefox, Edge | id = ID de empleado existente<br>idNomina = ID de nómina confirmada<br>Navegadores: Chrome, Firefox, Edge | 1. Generar el PDF desde Chrome y tomar captura del PDF visualizado<br>2. Generar el PDF desde Firefox y tomar captura<br>3. Generar el PDF desde Edge y tomar captura<br>4. Comparar visualmente las capturas entre navegadores | Documentar diferencias encontradas entre navegadores: formato, fuentes, alineación de campos, valores mostrados<br>Registrar si el PDF es visualmente consistente o si existen variaciones | Sin ejecutar | — |